# Maintenance Ledger

Unresolved decisions, deferred work, active limitations, and the dated evidence behind them. Durable rules live in `AGENTS.md`, shared guidance, a skill, a script header, or a test; remove an item here once its rule has moved there.

## Active Limitations

- OpenCode read adapter (2026-09-10, 1.18.30, Omarchy): direct and concurrent config/source Read and Glob calls completed from the non-Git family-directory launch; H confirmed no approval was needed. The installed-gate Read deny remained effective. Reading an owned external test directory did not grant write access: a subsequent native write asked, H rejected it, and the target remained absent; the fixture was removed. The 146 hermetic cases cover additional policy/lifecycle and Git/non-Git variants, not live Git-root or WSL acceptance. Headless rejection races and unexpected parent patterns remain native-handling limits. Revalidate through [operations](operations.md#opencode-read-approvals) after event/SDK, tool identity, policy merge, XDG layout or client changes, and on the next Git-root/WSL launch.

- Hermes runtime evidence (2026-09-09, Omarchy/mise 0.19.0): native status reported `gpt-6-astra (openai-codex)` and live replies succeeded; opaque checks confirmed configured xhigh, not effective request telemetry. Live checks covered guidance/skills, repository/system/temp reads, temporary native learning and removal, explicit-ID resume, and interactive background-child delivery with consistent parent/child cwd. The approval observer recorded the exact native callback's request/deny pair alongside its displayed timeout, with no target file created; narrative-only and target-lookalike cases are rejected by fixtures. The actual dispatcher independently refused synthetic protected reads/raw commits before backend invocation, with a successful ordinary-read control. Isolated Herdr 0.8.2 created a three-pane `hdw ha -c` workspace and delivered `-c` to a recorder in the correct cwd; H separately confirmed `hdw ha` in the running session. These are bounded observations, not OS containment. The model-driven canary remains UNVER when the model declines before hook evidence; do not force a prohibited call to manufacture a pass. Revalidate after relevant installed package, prompt, task-cwd, plugin/middleware, parser, approval or transport changes; [Access Policy](access.md#hermes-agent) owns the limits.
- Hermes v0.19 interfaces: project `.agents/skills` needs explicit loading through project references; shared global skills use `skills.external_dirs`. The ordinary `--cli chat --quiet --query` path retains normal policy; top-level `--oneshot PROMPT` bypasses approvals and is excluded. Top-level delegation is always background, ignoring `background: false`; a single-query process can exit before the result returns. Test asynchronous completion in an interactive session. The native model wizard writes the canonical Codex base URL, which reconciliation preserves; revalidate that constant with model-setup changes. Follow Omarchy's PyPI channel rather than a newer GitHub-only release.

- Local reference freshness (2026-09-06 inventory): the existing OpenCode clone is at `b578b7261fc9ec4917fe272df5cc4bd8a056cd5d` and lacks the `v1.18.29` tag used by earlier version-matched web source checks. The new Codex clone has `rust-v0.153.4` available and Claude Code's public-material clone is at `v2.1.263`; their creation does not establish every tool's implementation behavior. On the next source-dependent `/eyrsync` comparison, EyrAgents must use the preservation-first refresh preflight and establish the required OpenCode revision before claiming a local installed-release match. This reference-strategy bootstrap did not run a family-wide refresh or alter existing clones.
- Claude Code Bash read-rule coverage (official changelog checked 2026-09-06): [2.1.260 release notes](https://code.claude.com/docs/en/changelog) revert a 2.1.259 change applying Read denies to Bash arguments after false-positive blocks. Current permission docs still describe recognized file/redirect checks; the exact boundary is not established by native-file configuration contracts or the public support clone. EyrAgents owns a targeted synthetic, non-secret probe of affected operands/compound commands on the next Claude permission pass or relevant release. Until then, the access matrix labels this shell coverage version-dependent; no policy change or full-containment claim follows from the changelog alone.
- Access comparison baseline (2026-09-06): [`access.md`](access.md#evidence-and-refresh) records tracked configuration, current official semantics, and the installed/latest stable versions (Claude Code 2.1.263, Codex 0.153.4, OpenCode 1.18.29). This is not an effective-host or all-surface certification. EyrAgents `/eyrsync` owns revalidation when a dependent permission/default/matcher, approval flow, hook surface, plugin or sandbox backend changes; keep each tool's evidence date distinct if only part of the matrix can be checked.
- OpenCode native move destinations (source-checked 2026-09-06 against v1.18.29 `tool/apply_patch.ts`): destination external-directory checks run, but only source paths enter the edit permission request. Destination-only edit asks/denies can therefore be missed even in a Git worktree. The scratch plugin refuses moves involving its managed targets, not all moves. The [write-row qualification](access.md#move-destinations) owns current behavior; EyrAgents must revalidate when Apply Patch destination subjects or scratch preflight change. No live protected-target probe or runtime fix was performed; use separately checked operations, never this gap to bypass a restriction.
- OpenCode permissions (source-checked 2026-09-06 on 1.18.29, anomalyco/opencode `16747470`; plugin discovery checked at `b578b726`): location grants are not read-only. The approved OS and quarry reference exceptions reduce routine interruptions; the exact configured set lives in `AGENTS.md`. Shell file-command checks and grep do not enforce per-file read/edit policy, and arbitrary scripts, redirects or symlinks are not contained. Non-Git worktree `/` defeats the `../*` edit ask. External `always` spans the displayed directory across tools, native read/edit `always` uses `*`, and auto-approval removes ask interruptions. Auditor grep stays denied; scratch preflight is not TOCTOU/session isolation, and scratch Move-to remains refused because native destination edit checks are absent. Live checks verified managed scratch create/update/delete, persistent-scratch filename lookup, and auditor read/glob exposure. After restarting with the balanced policy, H confirmed no prompts for the OS-release read or Pacman-directory filename lookup on 2026-09-06. After the quarry-grant restart the same day, native Read of `~/Projects/quarry/opencode/README.md` and a README filename Glob both succeeded; H confirmed no prompts. This verifies scoped native access, not quarry writes/refreshes or every inheritance/escape case. Revalidate affected behavior after matcher, call-site, approval or plugin changes.
- OpenCode subagent delegation (source-checked 2026-09-04 on 1.18.26, `tool/task.ts`): the task tool checks the `task` permission against the requested subagent type and enforces `subagent_depth`, default 1, so a subagent cannot launch another; the `auditor` agent denies `task` as well. Recheck when that source changes.
- Claude Code user-level agents (observed 2026-09-04 on 2.1.260 (Claude Code)): a new agent under `~/.claude/agents` registered in the running session, but after its file was renamed the old name was dropped and the new one did not appear until a new session. Start a new session after adding or renaming an agent; recheck when the sub-agents docs mention live reload.
- OpenCode WebFetch (source-checked 2026-08-15 on 1.18.18, `tool/webfetch.ts`) has no SSRF boundary; sensitive reads and shell network stay separately restricted. Recheck when that source changes.
- The payload scanner recognizes a finite set of credential shapes; repository consent, not the scanner, is the disclosure decision.
- Scanner code-value false positives (2026-09-07): an ownership-field assignment using Python indexing and a quoted shell positional argument were classified as credential assignments because the fixture locals were named `token`. H approved descriptive ownership names after reviewing those exact non-secret spans; scanner policy was not weakened. On the next scanner-recognition change, assess these syntactic cases with negative credential-literal coverage before widening acceptance. Do not silently rename, redact or exempt an actual sensitive finding to pass a scan.
- Governance limits (local publication fixtures checked 2026-09-10): receipts and common-directory locks govern cooperating trusted-repository operations, not malicious hooks/config or same-user attackers. Commit apply preserves state when created-commit identification or exact-commit CAS is unsafe; worktree gate evidence does not attest a different index tree. Publication holds the lock across recheck and push, but cannot freeze noncooperating actors, DNS/SSH/client state or dependencies outside its bound inputs. Process-group cleanup covers handled timeout/cancellation, not SIGKILL, host loss or hostile descendants escaping the group. A surviving executing marker stays protected. Revalidate on Git transport/hook, process lifecycle or publication-scope changes.
- Close-out recovery limits (process-interruption fixtures checked 2026-09-09): exact-ID marker staging supports interrupted writes and payload/status removal, not tested physical power loss or hostile same-user races. Recovery requires the original compatible selection/disposition; unattributed legacy `.write-*` files remain outside automatic disposal. Inspect any actual orphan with H rather than sweeping the record store. Revalidate on record-format, atomic-write or filesystem changes.
- Unsupported governance cases still refuse: changed candidate gitlinks, configured push options/custom helpers, missing first-publication tracking baselines, unfinished Git operations, alternate caller indexes, missing commit reflogs, unsupported commit metadata, and ambiguous ff-only tails. Revisit with a concrete required workflow rather than bypassing checks. Unscannable blobs have bound manual-inspection metadata, and normal pre-push hooks have bound review fingerprints; neither substitutes for H's inspection or overrides an actual sensitive finding. Hook dependencies are not transitively fingerprinted; revalidate dependencies when their behavior matters. Explicit `--stage` can leave content staged after a scanner refusal; no receipt is created and nothing is auto-restored.
- Remote verification requires command-network access, unavailable inside Codex's normal profile; H or an already authorized network-capable context must run the exact bound verifier without weakening that profile. Opaque/custom SSH expressions may also make observation unavailable. These are honest unknown outcomes, not evidence that the push failed; revalidate when the network/transport interface changes and avoid repeated binds that cannot change the limitation.
- Reviewer provenance (source/fixtures checked 2026-09-05): Codex exec JSON exposes no effective model/effort/tier, so those fields are `unknown`; Claude result metadata can expose model and tier, but not effort. Strictly parsed client versions and safe identifiers are reported without configured-value fallback. Revalidate when client event schemas expose more reliable fields; live bridge verification of this implementation remains pending.
- Codex TOML preservation (13 synthetic preservation/refusal cases checked 2026-09-05): unsupported inline/dotted `hooks.state` layouts refuse with no replacement bytes. Host-owned semantics are checked before output, but this is not support for every valid TOML layout. Revisit with a non-secret synthetic reproduction when a real reconciliation refuses; never print host config values or replace it wholesale to get a pass.
- Codex sandbox (observed 2026-09-03 on 0.152.1): a literal denied workspace path creates an empty placeholder file wherever the path is absent, and a home-wide glob that matches inside an already denied directory or a runtime tree (`~/**/id_ed25519`, `~/**/.npmrc`) aborts or stalls startup. Profiles use `**/` globs in the workspace and deny directories, not their contents, under `$HOME`; recheck when the sandbox changes. Observed 2026-09-04 on 0.153.2 with `~/Projects` readable: `~/Projects/**/.npmrc` stalls startup because it matches inside `node_modules` trees, while the same glob for `.netrc`, `.pypirc`, and the four `id_*` names starts cleanly, so a project `.npmrc` stays readable by Codex; recheck with each sandbox change. Enforcement observed 2026-09-04 on 0.153.2 through `codex sandbox -P trusted-workspace -C <temp workspace> cat <path>`, which runs one command under the profile without the model: a fixture README under `~/Projects` reads, fixture copies of `.netrc`, `.aws/credentials`, and `.claude/.credentials.json` there are refused with EACCES, and a fixture `.npmrc` reads. Primary-session canary 2026-09-05 on 0.153.2: a sibling README under `~/Projects` reads without an approval request, and a request for `~/.ssh/config` is refused by the model on the guidance before any read. System trees observed 2026-09-05 on 0.153.2 through `codex sandbox` with a throwaway `CODEX_HOME`: literal grants on `/usr`, `/etc`, `/opt`, `/sys`, and `/var/lib/pacman` read, `/var/log` stays hidden, and `:minimal` already covers `/etc/os-release` and `/proc`. Skill scripts the same day on 0.153.4 through `codex sandbox -P trusted-workspace`: a script under `~/.agents/skills` both reads and executes under the profile's read grant, so Codex raises no ask before running a skill, and `~/.agents/hooks/commit-gate` is invisible inside the sandbox, as the hook runs outside it. Temp roots the same day: the baseline `:slash_tmp = write` read every credential-shaped fixture under `/tmp`, a literal `/tmp/opencode` or `/tmp/claude-1000` deny refuses with EACCES, and any glob under `/tmp` or `/var/tmp` (`/tmp/**/.env`, `/tmp/claude-*`) fails the sandbox with "ripgrep unreadable glob scan failed for /tmp" on the root-owned `systemd-private-*` entries, so those roots carry literal rules only.
- Codex skills (source-checked 2026-09-05 at openai/codex `8e4b7d3`, `exec-server/src/local_file_system.rs` walk and `ext/skills/src/loader/discovery.rs`): the walk skips every symlink that is not a directory and follows directory links under user and repository roots, and a skill is listed only from a regular-file `SKILL.md` entry, so each skill under `~/.agents/skills` deploys as one directory link; recheck when those sources change.
- Codex hook trust (observed 2026-09-04 and 2026-09-05 on 0.153.2; source-checked 2026-09-05 at openai/codex `8e4b7d3`, `hooks/src/engine/discovery.rs` and `config/src/hook_config.rs`): a user-config hook is trusted only while `hooks.state."<config path>:pre_tool_use:<group>:<handler>".trusted_hash` in `~/.codex/config.toml` equals the hook's current hash, and Codex writes that record after a session; the reconcile keeps the subtable under the template's hooks table and `check` ignores it, so a restow no longer resets the trust. Recheck when the hooks docs or the config reference name the key.
- OpenCode skill discovery (source-checked 2026-09-05 on 1.18.27, `skill/index.ts` and `effect/runtime-flags.ts`; live-checked the same day with `opencode debug skill`): `OPENCODE_DISABLE_EXTERNAL_SKILLS` drops the `.claude` and `.agents` sources, global and per project, while `OPENCODE_DISABLE_CLAUDE_CODE_SKILLS` drops `.claude` only, under which `~/.agents/skills` and a project's `.agents/skills` load natively with the same inventory and no duplicate warning; without either switch every harness skill is warned as a duplicate of its `.claude` copy, last wins. The siblings export the narrower switch, no repository carries `.opencode/skills` links, and `skills.paths` stays for the untrusted launch. Recheck when those sources change.
- OpenCode effort picker (source/isolated fixture checked 2026-09-06 on 1.18.29; H confirmed base Astra's `/variants` list with `Default` selected): base Astra and its derived Fast model both expose effort variants. The model picker skips the extra dialog when that exact model has a saved choice, including `Default`; base and Fast keep separate choices. `Default` leaves the configured model-level `xhigh` in effect on the standard request path, while an explicit named variant overrides it. The badge is not effective-request telemetry. No redundant agent pins or state rewrite is needed. Recheck when picker persistence, variant derivation or request precedence changes; upstream [#25126](https://github.com/anomalyco/opencode/issues/25126) and unmerged [#7156](https://github.com/anomalyco/opencode/pull/7156) describe the display/default distinction. No authenticated outgoing request was captured in this check.
- Claude Code status line (source-checked 2026-09-03 on 2.1.259, the newest release): the `rate_limits` payload carries only `five_hour`, `seven_day`, and the gateway `spend_limit`. The per-model Fable weekly window, tracked internally as `seven_day_overage_included` and shown by `/usage` as "Current week (Fable)", is not exposed, and the only client-side route to it calls the usage API with the token in the credential store, which the security boundaries forbid. Add a `fable:` segment after `7d:` when a release exposes that window; recheck when the status line docs or a changelog entry mention `rate_limits`.
- Model selection (source-checked 2026-09-05 against Claude Code's model page, openai/codex `588b781`, and OpenCode 1.18.29/models.dev): `fable` is the moving Claude alias; Codex's unset model follows the first picker-visible catalog entry, not a contractual strongest-model alias. Fast is a service tier, not a separate Codex picker model. OpenCode requires concrete IDs, currently `openai/gpt-6-astra` and `openai/gpt-5.4-mini`; its derived `-fast` variants are per-session choices. Recheck when the Claude alias table changes, the Codex catalog default moves away from the strongest listed model, or `opencode models` lists a newer generation; OpenCode pins are bumped by hand. A surviving host `service_tier` is H's preserved choice under the textual residue rule, not proof a reviewer used it. Obtain runtime evidence only through safe client output; unexposed fields stay unknown.

## Open Decisions

- Access parity follow-ups from the 2026-09-06 source comparison, owned by EyrAgents: Claude Code's credential-display soft-deny text permits a named-file exception contrary to stricter shared guidance, and its ordinary auto-mode reads can exceed standing authorization; Codex's short `auto_review.policy` replaces upstream configurable policy rather than appending to it, and rejects external writes even when guidance would permit a specifically authorized target. Decide these on the next explicit permission-policy pass with H, using the [matrix decisions](access.md#decisions-and-parity) and current official classifier/reviewer docs. These follow-ups remain unresolved differences, not approved permission relaxations.
- Deferred current decision, audit 8: explicit-target/completion-aware, reference-safe vault rename and promotion are not implemented. Current note operations and shared Obsidian plugin specs remain unchanged, owned by EyrArcHy/EyrWSL and the vault project, not this harness. Revisit only on H's explicit note-workflow request; the sibling ledgers retain the concrete scope.
- Deferred current decision, audit 21: immutable approved deployment snapshots for high-risk harness configuration are not implemented. Continue the live Stow model and watched-session rule; receipt immutability is not deployment immutability. Revisit only on H's explicit deployment-model decision.
- Deferred current decision, audit 22: new trust/disclosure tiers and per-vendor enrollment are not implemented. Preserve current trusted-repository and opt-out consent policy; private caller scratch and conservative reviewer permissions repair implementation boundaries without adding a new policy tier. Revisit only on H's explicit trust/disclosure decision.
- Whether an Omarchy-only Claude Code sandbox trial justifies tracked promotion. The untracked trial denies `~/.ssh`, `~/.aws`, and `~/.gnupg`, asks before `dangerouslyDisableSandbox`, checks command classification and repository writes, and needs a separate WSL behavior test. Upstream: anthropics/claude-code#43713, #26722, #54215. Decision input (2026-09-04): `bwrap` is installed here, and the sandbox's network default of localhost only would block the research fetches, `make refs`, `npm run build` font downloads, and `verify-published` that sessions rely on unless `sandbox.network.allowedDomains` lists each host; the trial's next step is a tracked allowlist and one session of ordinary work on Omarchy with it on.
- Whether Claude settings should define `fallbackModel`; current judgment is no, because a silent downgrade conflicts with the durable Fable alias.
- `features.js_repl = false` in the Codex template has no recorded reason; revisit when a task would benefit from the REPL. Source-checked 2026-09-05 at openai/codex `588b781`, `features/src/lib.rs`: `js_repl` is `Stage::Removed`, a flag kept for compatibility that does nothing, and `codex features list` on 0.153.4 reports it removed, so the line can go once H rules.

### Publication Access

Remaining acceptance (2026-09-11): [operations](operations.md#exact-approved-publication) owns exact-approved execution with the bound transport and host-local authentication. Local bare-remote fixtures cover publication state, hooks/tracking, drift, leases, failure after remote update, bounded output and handled interruption. Omarchy's standard gh/HTTPS authentication passed full-reboot API and private-repository Git checks; H confirmed normal Brave/terminal startup without extra prompts or manual credential repair. Actual WSL and real publication require their own evidence.

- Codex publication boundary (source-checked 2026-09-10): installed CLI 0.153.4 corresponds to local tag `rust-v0.153.4` at `3d2ee51ca2d5db578f328aa75e20aa22c0197c9a`; the relevant guard is also present at reference HEAD `5ecb3afd1bf405149e2159bfda50093b0c1b5fab`. Denied reads prevent native unsandboxed execution even after approval; no real publication was attempted to test this refusal. [Access policy](access.md#codex) owns the restriction-preserving handoff. Revalidate the versioned sandbox guard and native tool contract after a Codex approval/sandbox change or H's explicit request for a different route. Do not infer that prose approval supplies missing authority.
- Real `publish-apply` execution remains unobserved beyond local bare-remote fixtures. Authentication checks create no publication approval and public refs can be anonymous. At the next independently approved publication, follow the exact-binding execution and verification procedure; do not infer a real push result from API or read-only access. Revalidate on executor, host, credential-helper lifecycle, transport or verification-target changes.

## Deferred Work

- Hermes remaining host work is the actual WSL pass below. Effective effort remains unknown until a safe client interface exposes it; configured xhigh is not request telemetry. The live `spar-claude` bridge succeeded after its credential-inventory update, reporting Fable on standard tier with unknown effective effort; repeat only after a relevant bridge/client change. Native acceptance procedures live in [operations](operations.md#verify) and the maintained test helpers, not retained session scratch.
- Hermes WSL deployment: after pulling companion EyrAgents/EyrWSL changes, complete EyrWSL's Hermes install/update check, then `make dry-run`, `make restow`, and `make verify` here with PyYAML and Node available. Expect a private real config, the linked `eyragents` plugin, preserved SOUL/runtime state, additive shared skills and Astra/xhigh. Complete the live acceptance above on WSL; Omarchy source/fixture evidence does not close that host pass.
- Hermes reference bootstrap: `references.txt` declares `hermes-agent` at `https://github.com/NousResearch/hermes-agent.git`. No quarry clone was created during onboarding. On the next source-dependent `/eyrsync`, inventory `~/Projects/quarry/hermes-agent` and obtain explicit upstream/destination approval if missing; installed public package source remains usable for version-matched inspection. Do not silently substitute newer GitHub code for the mise/PyPI runtime.

- Narrow watch (2026-09-08): [mise, yadm, chezmoi, DotState, Dotbot, Mackup and rcm](https://jdx.dev/posts/2026-09-07-dotfiles-that-save-themselves/) versus our Stow/reviewed-Git workflow; revisit for material recovery/safety improvements. No adoption approved.
- Future enhancement (2026-09-08): shared, reproducible test images for local and hosted checks of exact staged candidates; revisit if environment-driven CI failures recur. Container access and image infrastructure require separate approval.
- Complete the remaining [publication access](#publication-access) acceptance on WSL and at the next independently approved publication; authenticated read-only access alone does not establish native execution.
- Workstream-artifact WSL pass: after pulling and verifying the intended EyrAgents deployment, create an owned ignored `.eyr-plans/<workstream>/` with private `audit/` and `spar/` directories. Confirm `review-brief --plan` can create a new Markdown brief in each, without source/index/ignore changes or gates; check both reviewer paths remain read-only. Verify native tool restrictions rather than assuming the metadata exception overrides them. Remove only the no-longer-needed test artifacts and empty directories, leaving user files and governance records intact. Revalidate after generator, tool permission, filesystem or lifecycle changes; remove this item after actual-host verification.
- Complete the [WSL host pass](#wsl-host-pass) on the actual host; its single procedure owns deployment, governance, OpenCode and behavioral checks without closing the other deferred items below.
- Reference coverage on WSL: on the next `/eyrsync` there, inventory the four EyrAgents manifest entries and existing quarry directories. Codex/OpenCode are client-source references; Claude Code is public release/plugin/support material only. Obtain explicit approval for any missing clone's upstream and destination, rather than assuming a sibling `make refs` will create an EyrAgents-only entry or treating Omarchy's clone state as cross-host evidence. Follow the skill's bootstrap/refresh preflight and preserve existing directories.
- Complete shell/hook-surface evidence on the next hook-dispatch or shell-tool change: Claude Code's tracked matcher covers Bash, not its separate Monitor tool; Codex's local `PreToolUse` coverage does not include hosted web or a fresh check of stdin to an existing process; OpenCode's plugin matches `bash`, not arbitrary custom tools. EyrAgents owns synthetic checks for the actual exposed surfaces, hook trust and timeout/error behavior before expanding the [consequential-action guarantees](access.md#consequential-actions). Lexical gates remain deliberately short of arbitrary-script containment; do not describe an untested route as enforced.
- Remaining runtime evidence after host gates (2026-09-06): agent-run `make restow verify` passed on Omarchy through the reviewed entrypoints, with host configuration kept opaque; Codex already matched the template and was not rewritten. Managed links/parents, installed hook and template-boundary checks passed. Fresh OpenCode marker/tool-exposure and location-prompt checks are recorded above. The broader behavioral canary, remaining targeted permission cases and WSL remain distinct outstanding evidence; neither this full-worktree host pass nor startup checks attest a different partial candidate tree.
- Rename the skills with an `eyr` prefix so they cannot collide with built-in or plugin commands; the form is undecided, H preferring the joined `eyrcommit` over `eyr-commit`. The rename touches the skill directories in the `agents` package and their Claude Code and OpenCode symlinks, the OpenCode command wrappers, the Codex template read grants, the `tests/prepare-stow.sh` fixture, and every cross-reference; deploy with `make clean`, `make restow`, and `make verify`, then remove the emptied skill directories under `$HOME`.
- An approval marker for Claude Code: a selector hook could bind H's approval to the immutable candidate ID in a location the agent cannot write, with `commit-apply` requiring it for Claude sessions. No marker is implemented; current approval remains conversational, with no equivalent selector-tool signal in Codex or OpenCode. Revisit on an explicit approval-enforcement request, not as a claim of unconditional branch safety.
- A Codex `auditor` role: `sandbox_mode = "read-only"` restricts only model-generated shell commands and is the legacy key the contracts test rejects beside `default_permissions`, so a role needs a read-only permission profile and must also disable `web_search`, apps, plugins, MCP servers, and nested agents; write it once those keys are confirmed against the config reference and verify the merged authority in a live Codex session.
- Give omasecboot a `check` target that runs `test`, so the gate contract covers it; wait until H's current work there is done.
- Behavioral evals for the skills through `claude plugin eval`: first scenarios are that free text never commits, an invalid message creates no receipt, candidate rejection preserves the receipt/index/worktree, and a push is presented only after applicable gates. Revisit when adding skill-behavior evaluation beyond the current fixtures and smoke canary.
- Run `/fewer-permission-prompts` against accumulated host transcripts and promote only durable read-only rules; keep `gh api` excluded.
- Revisit native cross-model review when a managed tool offers a read-only cross-vendor path under subscription authentication.
- Keep Codex-to-Claude review manual outside the strict Codex profile until its root-denied permissions can grant the Claude runtime and state without weakening the primary sandbox.
- File the two Codex sandbox limits upstream at openai/codex when H decides: a glob under a temp root fails the sandbox with "ripgrep unreadable glob scan failed for /tmp" on the root-owned `systemd-private-*` entries, and `~/Projects/**/.npmrc` stalls startup by matching inside `node_modules` trees. The reproductions are in the Codex sandbox item under Active Limitations; the agent drafts the reports on request and H posts them, since filing is externally visible.

## WSL Host Pass

Pending on the actual WSL host. Stop at the first mismatch; H approves package, exact-path cleanup, or host-local repair targets separately. Do not inspect credentials or print host configuration values. If the active tool policy blocks a named read-only diagnostic, report the exact command for H to run rather than changing permissions or using another route.

1. After H updates the intended clone, check `uname -r`, `id -u`, `git status --short`, `git diff --stat`, and `make require-clone`. The kernel must identify WSL2 and the deployed endpoints must belong to this clone. If UID is not 1000, stop to review the Codex template's literal `/tmp/claude-1000` deny rather than changing the account or claiming it covers another UID. Preserve all local work, including any retired tracked-config edits, for H's ruling.
2. Complete [EyrWSL's host pass](https://github.com/peregrinus879/eyrwsl/blob/main/docs/maintenance.md#wsl-host-pass) for mise wrappers and Windows interop; do not deploy EyrArcHy or repeat an already completed pass when its optional harness step links back here. Check `node --version`, `command -v claude codex opencode`, and `mise ls --current`. Client diagnostics below must use installed mise binaries, not stateful wrappers; if paths still resolve to wrappers, follow EyrWSL's fresh-shell activation checks first. Node belongs to EyrAgents' own prerequisites. No blanket launcher/package/auth cleanup is authorized.
3. In this EyrAgents clone, run `make dry-run`, `make check-skills`, `make restow`, then `make verify`, stopping on refusals. Confirm real managed parents, whole-directory `~/.agents/skills/{commit,publish,spar}` links, Claude's leaf skill links including `publish-apply`, executable skill scripts including `review-brief` and `publish-apply`, reachable `~/.agents/skills/commit/scripts/governance.py`, and the real installed commit gate matching the template. The verification suite must cover exact-ID publication execution/replay refusal, receipt close-out/recovery, raw index/link evidence, filter/lookup refusals and combined Make gates on this host, using disposable fixture roots rather than live receipts. Use explicit candidate/binding IDs with one private persistent record root; records never attest approval. Complete EyrWSL's standard gh/HTTPS onboarding and fresh-client/reboot checks, with H handling login and storage locally, then the applicable [publication acceptance](#publication-access). Actual receipt disposal follows the commit skill's preview and disposition rules, never a host-pass sweep. Recognized retired/dangling links can be handled by preparation; foreign entries must be preserved and reviewed.
4. Expect the host Codex config as a private regular file with template boundaries, preserved host tables and `hooks.state`, `personality = "none"`, and no retired model/subagent pins. Use the verifier and `stat -c '%a %h' ~/.codex/config.toml` metadata, not raw config output. Unsupported trust-state layouts require deliberate host-local repair. A surviving `service_tier` is preserved host choice, not mandatory cleanup; if H wants standard tier, use `/fast` deliberately. Confirm `codex features list` reports `fast_mode` available and the sandbox can execute its mise runtime. H handles any desired desktop-app plugin enablement locally.
5. Restart OpenCode and begin fresh agent sessions. Confirm native global `~/.config/opencode/AGENTS.md` resolves to shared guidance without a duplicate explicit instruction, all three permission plugins (`auditor-permissions.js`, `read-permissions.js`, and `scratch-permissions.js`) resolve into this clone, project `CLAUDE.md` fallback still works, skills include `commit`, `publish`, `spar`, and local `eyrsync`, and both auditor configurations load their shared charter. Confirm `opencode models` lists the configured primary model and its Fast variant. Using only synthetic non-secret fixtures, verify auditor read/glob with unknown/grep/edit denies and ordinary native scratch Add/Update/Delete under `/tmp/opencode`. Complete [read-adapter acceptance](operations.md#opencode-read-approvals), including retained external-write and explicit-policy prompts. Check prompt-free OS-release Read, Pacman-metadata filename lookup, and ordinary quarry reads. Record actual behavior, not OS/race/session containment.
6. Run one live `spar-codex review` from Claude Code and one `spar-claude review` from OpenCode with a small non-sensitive artifact in an explicitly selected private `TMPDIR`; allow the bridge timeout. Record safe returned IDs/provenance and any `unknown` fields, not transcript/config/auth contents. Confirm runtimes resolve outside repository/temp roots.
7. Run `make canary` and retain its actual status: 1 failed, 2 skipped/unverified, 0 behavioral smoke only. It makes up to six calls per tool and never resets a moved fixture HEAD. OpenCode performs the preapproved OS-release read; its general external-temp read still requires interactive approval and is skipped. Validate that remaining prompt separately rather than forcing a zero exit or bypassing approvals. Other missing or unverified checks keep the host pass open.
8. Check that `publish-clip` without arguments reports `clipboard: unchanged (copy not requested)`. Only after H explicitly requests a copy and chooses disposable text, check `publish-clip --copy`, separately from EyrWSL's Neovim provider. Report the backend actually selected: Wayland has priority when available; Windows interop otherwise selects `clipboard: clip.exe`. Confirm preparation/re-display leaves the clipboard alone and the Copy choice does not start verification. Report safe client versions, tested revision, completed checks and remaining failures. Remove this host-pass section and its pointer only after every check succeeds or each remaining limitation is explicitly retained.

## Revalidation Triggers

- When a Claude Code release reads `AGENTS.md` or `~/.agents/skills` natively (announced by Anthropic in late August 2026 as "more hackable", without a date): run `/context` and `/skills` for a guidance file loaded twice or duplicate skill entries, then remove the root `CLAUDE.md` import shim or the `claude-code/.claude/skills` symlinks accordingly.
- Run `make canary` after restow or relevant interface changes, with up to six calls per tool and explicit incomplete results. OpenCode's deliberate interactive-only checks need separate prompt verification, not a headless bypass. Model-reported outcomes are behavioral evidence; fixture/static passes do not replace installed dispatch. Retain unresolved failures with their triggers, not a growing success history.
- Run `/eyrsync` when a tool release changes a configuration key, hook event, agent or skill field, permission rule/default or exposed tool surface, when any managed tool makes a major/breaking release, when references are missing/stale or evidence conflicts, when the harness changes something a sibling depends on, or periodically. Start with coverage for all four tools, then reconcile decisions, implementation, docs/releases, available version-matched source/public material and runtime evidence within scope. Keep unresolved gaps here instead of silently changing grants or claiming unavailable source proof.
- Routine client releases need no ledger update; revalidate when a dependent interface, permission rule, temp root, matcher, model catalog, or observed behavior changes. Evidence uses `claude --version` and `mise ls --current` so stateful wrappers are not invoked.
- Re-run `make check` after changing a bridge, scanner, permission profile, or runtime dependency; run one live review per bridge after reviewer-flag or `claude -p`/`codex exec` behavior changes, recording safe provenance and unknowns. Old bridge success does not validate the current private-artifact/provenance implementation.
- Re-run the Codex root-deny, workspace, network, plugin-isolation, and resumed-session checks after changing its permission profile or runtime boundaries; `codex sandbox -P <profile> -C <dir> <command>` tests the filesystem rules directly, since the model refuses credential-shaped reads on its own before the sandbox sees them.
- Re-run the OpenCode project-isolation, app-temp, and permission-subject checks after a relevant OpenCode change; the project-disabled launch in [access](access.md#untrusted-checkouts) depends on `OPENCODE_DISABLE_PROJECT_CONFIG` and `OPENCODE_DISABLE_EXTERNAL_SKILLS` continuing to exist.
