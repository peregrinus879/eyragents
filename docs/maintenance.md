# Maintenance Ledger

Unresolved decisions, deferred work, active limitations, and the dated evidence behind them. Durable rules live in `AGENTS.md`, global guidance, a skill, a script header, or a test; remove an item here once its rule has moved there.

## Active Limitations

- Permission model acceptance (2026-09-24, Omarchy, pending): reads run everywhere except secrets and personal folders; remote-changing, destructive, commit-producing and publishing commands ask. The headless canary after deployment passed 15 of 16 checks. Remaining: after an OpenCode restart, run [permission acceptance](operations.md#permission-acceptance) in both tools, with H confirming and declining each native prompt, including one `git commit` and one `git push`. Remove this item after acceptance; WSL is in the host pass.
- Local reference freshness (revision/tag inventory rechecked 2026-09-15): the 2026-09-14 clean checkouts were observed at Claude public material `ab9b2cf7bb9e4f98ff264c07a22e46d83c29c558` and OpenCode `b578b7261fc9ec4917fe272df5cc4bd8a056cd5d`; those revisions remain unchanged. The requested Claude 2.1.270–272 and OpenCode 1.18.30–31 tags remain unavailable locally, so the scoped compatibility review used versioned public source/material. Use the bounded reference lifecycle when local freshness is needed. No clone was created, fetched or repointed by either pass.
- Claude Code Bash read-rule coverage (public changelog checked through 2.1.272, 2026-09-15): 2.1.260 reverted broad argument checks; 2.1.268 added symlink deny/ask consistency and 2.1.269 added `tee` destination checks. The [2.1.271 operand/flag and optional directory-fence fixes](access.md#claude-code) need scoped synthetic native acceptance on 2.1.272 using [operations](operations.md#claude-compatibility-acceptance). Public documentation/changelog and native Read/Edit or classifier fixtures do not close shell operands, redirections, compound commands or arbitrary subprocess coverage. Retain the source/fixture/live distinction and never force a prohibited call merely for evidence.
- Claude Code continuity and hand-back acceptance (2.1.272, 2026-09-15 public-material review): the changed classifier-reviewed subagent return and resumed file-read tracking need [targeted native acceptance](operations.md#claude-compatibility-acceptance), alongside fresh guidance/skill discovery. The [access owner](access.md#claude-code) records current semantics and the unspecified 2.1.272 reliability delta. Revalidate after those interfaces or the managed agent/skill change; prior 2.1.270 live results do not attest them.
- Access baseline: [`access.md`](access.md#evidence-and-refresh) was reconciled on 2026-09-24 against Claude Code 2.1.281 documentation and OpenCode 1.18.32 source. Revalidate after matcher, approval, hook, filesystem or policy changes; live activation and WSL remain separate.
- OpenCode native move destinations (source-checked 2026-09-24 against 1.18.32): Apply Patch checks a `Move to` destination against `external_directory` only, so an Edit deny such as `.git/**` does not bind it. The shell reaches the same paths, so no reach is added. [Move destinations](access.md#move-destinations) holds the source; revalidate when Apply Patch permission subjects change.
- OpenCode permission limits (source-checked 2026-09-24 on 1.18.32): read and edit subjects are worktree-relative, so a non-Git worktree (`/`) defeats the `../*` edit ask, and `/tmp/opencode` edits ask from a worktree under `/tmp`. Shell file-command checks and grep do not enforce per-file read or edit rules, and arbitrary scripts are not contained. External `always` approvals span the displayed directory; native read/edit `always` uses `*`. Revalidate after matcher, call-site or approval changes.
- OpenCode subagent delegation (source-rechecked 2026-09-15 on 1.18.31, [`tool/task.ts`](https://github.com/anomalyco/opencode/blob/v1.18.31/packages/opencode/src/tool/task.ts)): the task tool checks the `task` permission against the requested subagent type and enforces `subagent_depth`, default 1, so a subagent cannot launch another; the `auditor` agent denies `task` as well. Recheck when that source changes; source compatibility does not establish fresh delegation dispatch.
- Claude Code user-level agents (observed 2026-09-04 on 2.1.260 (Claude Code)): a new agent under `~/.claude/agents` registered in the running session, but after its file was renamed the old name was dropped and the new one did not appear until a new session. The [current documentation](https://code.claude.com/docs/en/sub-agents#write-subagent-files), reviewed 2026-09-15, describes live add/edit detection in existing watched directories, with exceptions for new directories, added-directory agents and disabled slash commands. Rename acceptance remains unverified; retain the restart guidance until a scoped owned-agent check resolves the older observation.
- OpenCode WebFetch (source-checked 2026-08-15 on 1.18.18, `tool/webfetch.ts`) has no SSRF boundary; sensitive reads and shell network stay separately restricted. Recheck when that source changes.
- Remote verification: opaque/custom SSH expressions may make observation unavailable. These are honest unknown outcomes, not evidence that the push failed; revalidate when the network/transport interface changes and avoid repeated binds that cannot change the limitation.
- Reviewer bridges (2026-09-24, Omarchy): live smoke reviews ran through both bridges (see the Change A record). Run one live review per bridge after a bridge, charter or client CLI change; reviewer-reported capabilities are behavior evidence, not dispatch proof.
- OpenCode skill discovery (source rechecked 2026-09-24 against [1.18.32](access.md#opencode)): the external-skills switch drops `.claude` and `.agents` discovery; the narrower Claude-skills switch drops only `.claude`. The mise fragment supplies the narrower default, and `skills.paths` keeps the shared skills in the restricted launch; discovered paths are de-duplicated. Revalidate after loader/flag or startup-path changes; WSL acceptance stays separate.
- OpenCode effort picker (source/isolated fixture checked 2026-09-06 on 1.18.29; H confirmed base Astra's `/variants` list with `Default` selected): base Astra and its derived Fast model both expose effort variants. The model picker skips the extra dialog when that exact model has a saved choice, including `Default`; base and Fast keep separate choices. `Default` leaves the configured model-level `xhigh` in effect on the standard request path, while an explicit named variant overrides it. The badge is not effective-request telemetry. No redundant agent pins or state rewrite is needed. Recheck when picker persistence, variant derivation or request precedence changes; upstream [#25126](https://github.com/anomalyco/opencode/issues/25126) and unmerged [#7156](https://github.com/anomalyco/opencode/pull/7156) describe the display/default distinction. No authenticated outgoing request was captured in this check.
- Claude Code status line (source-checked 2026-09-03 on 2.1.259, the newest release): the `rate_limits` payload carries only `five_hour`, `seven_day`, and the gateway `spend_limit`. The per-model Fable weekly window, tracked internally as `seven_day_overage_included` and shown by `/usage` as "Current week (Fable)", is not exposed, and the only client-side route to it calls the usage API with the token in the credential store, which the security boundaries forbid. Add a `fable:` segment after `7d:` when a release exposes that window; recheck when the status line docs or a changelog entry mention `rate_limits`.
- Model selection (source-checked 2026-09-05 against Claude Code's model page and OpenCode 1.18.29/models.dev): `fable` is the moving Claude alias. OpenCode requires concrete IDs; its derived `-fast` variants are per-session choices. Recheck when the Claude alias table changes or `opencode models` lists a newer generation; OpenCode pins are bumped by hand. Obtain runtime evidence only through safe client output; unexposed fields stay unknown.

## Open Decisions

- Deferred current decision, audit 21: immutable approved deployment snapshots for high-risk harness configuration are not implemented. Continue the live Stow model and watched-session rule. Revisit only on H's explicit deployment-model decision.
- Whether an Omarchy-only Claude Code sandbox trial justifies tracked promotion. The untracked trial denies `~/.ssh`, `~/.aws`, and `~/.gnupg`, asks before `dangerouslyDisableSandbox`, checks command classification and repository writes, and needs a separate WSL behavior test. Upstream: anthropics/claude-code#43713, #26722, #54215. Decision input (2026-09-04): `bwrap` is installed here, and the sandbox's network default of localhost only would block the research fetches, `make refs`, `npm run build` font downloads, and `verify-published` that sessions rely on unless `sandbox.network.allowedDomains` lists each host; the trial's next step is a tracked allowlist and one session of ordinary work on Omarchy with it on.
- Whether Claude settings should define `fallbackModel`; current judgment is no, because a silent downgrade conflicts with H's chosen primary model.

### Publication Access

The remaining host acceptance is the [WSL host pass](#wsl-host-pass), including host-local gh/HTTPS onboarding and a first push through the native prompt there. The [ship skill](../agents/.agents/skills/ship/SKILL.md) owns the procedure.

## Deferred Work

- Codex and Hermes retirement on WSL (2026-09-24, pending): Omarchy is done. On WSL, follow the retirement section of the [WSL handoff](../../eyrwsl/docs/handoff.md) after pulling EyrAgents, EyrArcHy and EyrWSL; it owns the exact steps and acceptance checks. Remove this item once that section is complete.

- Persistent-scratch relocation and WSL acceptance (2026-09-21, pending): the configured scratch grant now targets `~/Projects/eyrie/scrape`. Coordinate the intact move from `~/Projects/scratch` and repository relocation through the [WSL projects-layout handoff](../../eyrwsl/docs/handoff.md), preserving existing directory modes and neighboring work. After the physical layout is verified on WSL, run `make restow verify` from the intended EyrAgents clone, restart OpenCode and the other affected clients, and complete [persistent-scratch acceptance](operations.md#persistent-scratch). Verify the new root and intermediate `eyrie` ancestry, retained protected-path refusals, and ordinary external handling for the old root and `eyrie` siblings outside the active workspace. Earlier host results do not attest this relocation; retain the native limitations above. Remove this item only after actual WSL relocation and acceptance.

- Permission model WSL acceptance: after H pulls, run `make restow verify` (preparation prunes the retired `~/.config/opencode/lib`), restart the clients, and run [permission acceptance](operations.md#permission-acceptance) and bounded canaries with owned non-secret fixtures. Remove this item after actual-host acceptance.

- Reference migration WSL acceptance: after pulling and running `make restow verify`, restart the clients and run `make lint check` with GitHub CLI metadata access available. Preview the selected existing references with `bash scripts/update-references.sh --dry-run NAME...`; expect pinned-ID checks and no clone/manifest writes. On a task-required refresh, confirm preserved local work/tags/push URLs and any verified canonical URL reconciliation. Native asks/network limits remain binding; synthetic Git/GitHub fixtures establish the procedure, not prompt-free live execution or authority for different projects. Remove this item after actual-host acceptance.

- Standalone startup WSL pass: after pulling this repository, follow [standalone setup](setup.md), then run `make restow verify`. In a fresh mise-activated shell, check the two OpenCode startup defaults without relying on host-dotfiles exports; also check a `mise exec` launch with those inherited variables unset. Expect `OPENCODE_DISABLE_CLAUDE_CODE_SKILLS=1` and `OPENCODE_ENABLE_EXA=1`, with explicit caller overrides preserved. Restart OpenCode and confirm normal skill discovery and web-search availability. Keep actual WSL/native evidence distinct from the isolated `tests/mise-env.sh` checks; remove this item after the host pass.

- Narrow watch (2026-09-08): [mise, yadm, chezmoi, DotState, Dotbot, Mackup and rcm](https://jdx.dev/posts/2026-09-07-dotfiles-that-save-themselves/) versus our Stow/reviewed-Git workflow; revisit for material recovery/safety improvements. No adoption approved.
- Future enhancement (2026-09-08): shared, reproducible test images for local and hosted checks of exact staged candidates; revisit if environment-driven CI failures recur. Container access and image infrastructure require separate approval.
- Complete the remaining [publication access](#publication-access) acceptance on WSL; authenticated read-only access alone does not establish native execution.
- Complete the [WSL host pass](#wsl-host-pass) on the actual host; its single procedure owns deployment, OpenCode and behavioral checks without closing the other deferred items below.
- Reference coverage on WSL: on the next `/eyrsync` there, inventory the manifest entries and existing quarry directories. OpenCode is a client-source reference; Claude Code is public release/plugin/support material only. Obtain explicit approval for missing clones; this repository's updater refreshes existing entries only. Do not treat Omarchy's clone state as cross-host evidence.
- Give omasecboot a `check` target that runs `test`, so its gates cover it; wait until H's current work there is done.
- Behavioral evals for the skills through `claude plugin eval`: first scenarios are that free text never commits, a commit runs only after its card and at the native prompt, and a push waits for H's go. Revisit when adding skill-behavior evaluation beyond the smoke canary.
- Run `/fewer-permission-prompts` against accumulated host transcripts and promote only durable read-only rules; keep `gh api` excluded.
- Revisit native cross-model review when a managed tool offers a read-only cross-vendor path under subscription authentication.

## WSL Host Pass

Pending on the actual WSL host. Stop at the first mismatch; H approves package, exact-path cleanup, or host-local repair targets separately. Do not inspect credentials or print host configuration values. If the active tool policy blocks a named read-only diagnostic, report the exact command for H to run rather than changing permissions or using another route.

1. After H updates the intended clone, check `uname -r`, `git status --short`, `git diff --stat`, and `make require-clone`. The kernel must identify WSL2 and the deployed endpoints must belong to this clone. Preserve all local work, including any retired tracked-config edits, for H's ruling.
2. Complete [standalone setup](setup.md) on the normal-user WSL account. Check `node --version`, `command -v claude opencode`, and `mise ls --current`. Use mise activation/shims or `mise exec` so the harness-owned startup fragment applies. Diagnostics should use installed binaries rather than lazy installers. Check Windows interop separately if using `clip.exe`; no host-dotfiles checkout or launcher/package/auth cleanup is required.
3. In this EyrAgents clone, run `make dry-run`, `make check-skills`, `make restow`, then `make verify`, stopping on refusals. Confirm real managed parents, whole-directory links `~/.agents/skills/{ship,spar}` and `~/.claude/skills/{ship,spar}`, executable spar bridges, and that preparation pruned the retired `commit`, `publish` and `develop` skill directories. Delete the retired installed gate `~/.agents/hooks/commit-gate` and its then-empty directory (no configuration references them). Complete this repository's [GitHub onboarding](setup.md#github-access), with H handling login and storage locally. Recognized retired/dangling links are handled by preparation; foreign entries must be preserved and reviewed.
4. Restart OpenCode and begin fresh agent sessions. Confirm native global `~/.config/opencode/AGENTS.md` resolves to global guidance without a duplicate explicit instruction, no plugins or command wrappers remain under `~/.config/opencode`, `~/.config/opencode/lib` is gone, Claude Code 2.1.277 or later reads the project `AGENTS.md` natively (no `CLAUDE.md` in the repositories), skills include `ship`, `spar`, and local `eyrsync`, and both auditor configurations load their shared charter. Confirm `opencode models` lists the configured primary model and its Fast variant. Run [permission acceptance](operations.md#permission-acceptance) with synthetic non-secret fixtures, including an auditor that runs a read-only shell command and has no edit tools. A real CI retry follows [CI reruns](operations.md#ci-reruns) only when needed. Record actual behavior, not OS/race/session containment.
5. Run one live review in each direction, `spar-claude` from OpenCode and `spar-opencode` from Claude Code, on a small non-sensitive request. Record the session ID and model, not transcript, configuration or authentication contents.
6. Run `make canary` and retain its actual status: 1 failed, 2 skipped/unverified, 0 behavioral smoke only. It makes up to six calls per tool and never resets a moved fixture HEAD. Missing or unverified checks keep the host pass open; never force a zero exit or bypass an approval. Report safe client versions, tested revision, completed checks and remaining failures. Remove this host-pass section and its pointer only after every check succeeds or each remaining limitation is explicitly retained.

## Revalidation Triggers

- When a Claude Code release discovers `~/.agents/skills` natively (checked 2026-09-24 against the skills documentation: it reads only `.claude/skills`): run `/skills` for duplicate entries, then remove the `claude-code/.claude/skills` symlinks. Project `AGENTS.md` is read natively since 2.1.277, so the root `CLAUDE.md` imports were removed.
- Run `make canary` after restow or relevant interface changes, with up to six calls per tool and explicit incomplete results. Model-reported outcomes are behavioral evidence; fixture/static passes do not replace installed dispatch. Retain unresolved failures with their triggers, not a growing success history.
- Run `/eyrsync` when a tool release changes a configuration key, hook event, agent or skill field, permission rule/default or exposed tool surface, when a managed tool makes a major/breaking release, when references are missing/stale or evidence conflicts, or periodically. Reconcile both tools' coverage, decisions, implementation, official sources and runtime evidence within scope. Keep unresolved gaps here instead of silently changing grants or claiming unavailable source proof.
- Routine client releases need no ledger update; revalidate when a dependent interface, permission rule, temp root, matcher, model catalog, or observed behavior changes. Evidence uses `claude --version` and `mise ls --current` so stateful wrappers are not invoked.
- Re-run `make check` after changing a bridge, the charter, a permission rule or a runtime dependency, and run one live review per bridge after a bridge, charter or client CLI change.
- Re-run the OpenCode project-isolation, app-temp, and permission-subject checks after a relevant OpenCode change; the project-disabled launch in [access](access.md#untrusted-checkouts) depends on `OPENCODE_DISABLE_PROJECT_CONFIG` and `OPENCODE_DISABLE_EXTERNAL_SKILLS` continuing to exist.
