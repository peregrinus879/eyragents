# AGENTS.md - EyrAgents

EyrAgents is a GNU Stow repository for shared Claude Code, Codex, OpenCode, and Hermes Agent configuration. Keep changes portable across Omarchy and WSL unless a host-specific scope is explicit.

## Loading and Ownership

- Claude Code loads `~/.claude/CLAUDE.md`, its documented user instruction file and a tracked symlink to shared guidance, then this file through the root `CLAUDE.md` import. Codex loads `~/.codex/AGENTS.md`, a tracked symlink to shared guidance, and this file. OpenCode loads shared guidance through its native global `~/.config/opencode/AGENTS.md` symlink and this file; no explicit `instructions` duplicate is configured, and project `CLAUDE.md` fallback remains enabled.
- `agents/.agents/shared-guidance.md` is the canonical cross-tool policy and `agents/.agents/skills/*/SKILL.md` are the canonical skills, in the tool-neutral package that deploys `~/.agents`, the Agent Skills standard's home, which Codex reads natively. Claude Code, Codex and OpenCode use tracked global-instruction symlinks; Hermes composes that neutral source into its stable environment hint. Claude Code's `~/.claude/skills/<name>` entries hold tracked symlinks to each skill's `SKILL.md` and scripts; OpenCode's `skills.paths` names the deployed skills directly. The skills' executables live where the Agent Skills specification puts them, `skills/<name>/scripts/`: `commit-candidate`, `commit-apply`, and the shared `governance.py` implementation under `commit`, `publish-bind`, `publish-verify`, and `publish-clip` under `publish`, `review-brief`, the bridges, and the scanner under `spar`; `make stow` installs `templates/hooks/commit-gate` as a real file. The skills call the scripts, so the procedure is code and the prose is intent. Order tools as Claude Code, Codex, OpenCode, Hermes Agent.
- This file owns repository invariants. `README.md` is the public overview and navigation entry point; `docs/setup.md` owns installation/deployment and `docs/operations.md` owns usage/verification. `docs/design.md` owns architecture and rationale. `docs/access.md` owns the cross-tool access comparison, restricted launches, implementation references, and official-source baseline; read it first for access questions and reconcile it through `/eyrsync`. Skills own workflow procedure and script headers own local constraints. `docs/maintenance.md` holds unresolved decisions, deferred work, active limitations, and dated revalidation evidence. Update each fact at its owner and link to it; keep README detail proportional. Git history owns provenance.
- [Workstream Checkpoints](agents/.agents/shared-guidance.md#workstream-checkpoints) owns the primary's ignored checkpoint, `audit/` and `spar/` files and their dependency-aware cleanup; reviewers stay read-only. Generic deferred issues belong in canonical docs, not completed workstream archives. Receipt close-out is separate from scratch cleanup and owned by the commit skill. A tracked `docs/handoff.md` carries concrete cross-host work, never approval, and is deleted or updated when complete.
- Root wrappers and documentation are source-only; Stow deploys package contents. Because the packages are live configuration on a stowed host, an edit to a settings file, skill, or bridge here is active for the next session of that tool before any commit; work on this repository only in a session H is watching.

## Security Boundaries

- Authentication, credentials, session state, and generated host state stay out of Git. The tracked Claude settings are the only app-managed rewrite exception; preserve and review those rewrites instead of reverting them.
- Read/edit policies deny credential stores; enforcement by search tools and the shell is not equivalent. Codex's sandbox, Claude Code's classifier, and OpenCode's directory-only shell/search checks have the limitations below, and the model's secret-material rule still applies. The configured inventory stays aligned across the four tools, reviewer bridges, and scanner: `~/.ssh`, `~/.aws`, `~/.gnupg`, `~/.kube`, provider auth files, keyrings, browser profiles, shell history, `.env` and `.env.*`, `.netrc`, `.npmrc`, `.pypirc`, `secrets/`, `credentials` and `credentials.*`, `auth.json`, OpenSSH `id_*` keys, and `*.key`, `*.pem`, `*.p12`, `*.pfx`. The Codex agent has no read grant on `~/.codex/config.toml`, whose host tables may carry MCP credentials. Names such as `credentials-policy.md` stay readable. Contracts check policy alignment and behavioral path cases, not universal runtime containment.
- Git internals are protected in layers: native rules deny `.git/**` writes on checked targets, subject to OpenCode's documented [Move-to destination gap](docs/access.md#move-destinations); Codex keeps `.git/config` and `.git/hooks` read-only at the OS level, Claude Code's classifier policy requires H's explicit instruction for Git configuration, remote, hook, and alias changes, and OpenCode asks before the named command forms. These changes run code on H's next Git command and never appear in a diff, which is why they get their own rules.
- Claude Code's and Codex's `PreToolUse` hooks and the OpenCode commit-gate plugin run `commit-gate` before shell commands. It denies commit-producing Git forms, aliases, opaque subcommands, and ambiguous input; `merge`/`pull --ff-only` exemptions require unambiguous literal options and reject conflicting ff, rebase, squash, and bypass options. This is shell-text enforcement, not containment of scripts hiding Git operations. Raw Git commands H wants run are H's own through `!`; prose containing them is written with file tools. The installed gate is a real copy under `~/.agents/hooks`, outside workspaces because Codex hooks execute outside its sandbox; file tools may not edit it. Enabling Codex hooks also loads project-local hooks in trusted projects, so untrusted checkouts use the launches in `docs/access.md#untrusted-checkouts`.
- `commit-candidate` records the prepared index by default; explicit `--stage` stages whole literal intended paths only after mixed-file and path preflight. Immutable SHA-256 receipts bind tree, parent, branch, message, identity and worktree; mutable status and common-Git-directory locks govern cooperating operations. Apply/show/clear/close/publication operations require exact IDs, never a mutable latest record. `--close ID... --dry-run` previews separate receipt disposal; committed candidates require selected eligible publication ancestry, and `--accept-unverified` needs H's accepted close-out without a success claim. Preserve active/ambiguous/corrupt/foreign-worktree state. Transient closing markers make payload-then-status deletion resumable; retain stable lockfiles, not receipt archives. Receipts and close-out do not attest approval, gates or fresh remote state.
- `commit-apply ID` uses an isolated index and normal hooks, then validates the actual tree, complete parent list, message, author/committer, and branch position. Rejection never restores the original index or worktree. Compensation is conditional: only the uniquely identified commit created by this invocation, still on the recorded branch with its recorded sole parent, may be compare-and-swapped back. Ambiguous evidence, moved checkout/tip, or failed CAS preserves state and stops for H. These are trusted-repository governance checks, not an unconditional branch-advance or same-user isolation guarantee.
- `publish-bind` scans new commit metadata, explicit per-parent merge patches, the flat diff, and complete newly reachable text blobs and tree paths. Unscannable objects have digest-bound metadata and require H's inspection, never a fabricated complete scan. Normal executable pre-push hooks are inventoried and content-fingerprinted for review, not skipped; unsupported/sensitive hook targets and configured scope-expanding options/helpers refuse. The immutable binding names the resolved endpoint and exact branch/base/reviewed commit plus configuration. Its H-run command carries the record root and `--check ID` preflight, configured remote name for normal tracking/hook semantics, explicit refspec, exact-base lease, and tag/submodule suppression. `publish-verify ID` observes the bound endpoint through read-only `ls-remote`; tracking, CI and optional deployment remain distinct. A mismatch does not prove the push never landed. Missing first-publication baselines need a separate full-history procedure.
- Trusted-repository defaults are not an isolation boundary against hostile project configuration. Use the restricted launches in `docs/access.md#untrusted-checkouts` for untrusted checkouts.
- Primary web research follows each tool's managed policy: Claude Code and OpenCode keep web fetch and search, and Codex keeps live web search plus its desktop-app apps, browser, hooks, and plugins. These are egress and authority surfaces for a prompt-injected session, kept by H's choice. Reviewer web tools and command network remain disabled.

## Tool Configuration

- Hermes Agent follows Omarchy's mise/PyPI installation, with v0.19.0 as the compatibility baseline. The default profile uses `openai-codex` / `gpt-6-astra` at `xhigh`, native `coding_context: auto`, local terminal, smart approvals, memory, skill learning, curator and ordinary delegation. Keep the native toolset; do not substitute a coding-only allowlist or Codex app-server runtime. Account entitlement and effective model/effort require safe runtime evidence.
- `templates/hermes/config.yaml` owns explicit leaves in a private, real host-local `~/.hermes/config.yaml`; `scripts/reconcile-hermes-config.py` preserves other parsed host values and merges skill-library/plugin lists additively. It composes canonical shared guidance into the stable `agent.environment_hint`, preserving Hermes's own `SOUL.md` and native project-context precedence. Shared skills load through `skills.external_dirs`. Only the default profile is deployed; profile state, secrets, identity, memory and generated skills stay outside Git. YAML comments/formatting are app-owned; ambiguous/unsafe host layouts refuse without displaying values.
- `hermes/.hermes/plugins/eyragents` carries the native-tool guard. It normalizes local task-relative file operands, checks read/write/move targets, adds credential filtering to native search results, protects shared skills from `skill_manage` mutation, and routes terminal commands to the installed commit gate. Hooks and execution middleware retain useful Python, subprocess, process-input and delegation capabilities with explicit coverage limits in `docs/access.md`; no OS isolation or fail-closed plugin-loading claim is made. Remote backends require a separate access review. Native learning owns its stores, not shared policy. The Astra session uses `spar-claude` for cross-vendor review; no Hermes auditor or reviewer bridge is configured.

The [access matrices](docs/access.md) compare the four tools without equating guidance, native permission, classifier review, sandbox enforcement, and live evidence. A policy or tool-surface change keeps its matrix cells, implementation references and rationale current; unresolved parity gaps belong in the ledger. Do not broaden a stricter boundary merely to obtain identical cells.

Every managed tool has a declared reference/evidence strategy, owned by [eyrsync's Sources and Reference Lifecycle](.agents/skills/eyrsync/SKILL.md#sources). `references.txt` declares Claude Code's public release/plugin/support repository and Codex/OpenCode/Hermes client source repositories. Do not claim proprietary Claude CLI implementation evidence from its support clone, or infer installed/hosted behavior from any public client checkout. Inventory missing/stale references and reconcile docs, releases, available source and runtime evidence within the agreed scope; initial creation and preservation-sensitive effects retain their explicit approval boundaries.

OpenCode's native-edit statements below describe checked permission subjects: Apply Patch does not submit Move-to destinations to the edit check. Its external destination check remains, and the scratch plugin refuses moves only within its managed scope. The access matrix's M qualification applies; no documented gap authorizes bypassing a restriction.

- Claude Code runs in auto mode with bypass disabled and no tracked sandbox, with standing read allows on `~/Projects/**`, the temp roots `/tmp/**` and `/var/tmp/**` less OpenCode's session root `/tmp/opencode`, and the system trees `/usr`, `/etc`, `/opt`, `/sys`, and `/var/lib/pacman`, over which the deterministic credential denies still win; deterministic denies cover the named command forms of pushes, `git clean`, `gh` repository mutations, and privilege escalation, plus credential reads and writes by file tools, while alternate spellings, destructive Git operations, Git configuration changes, other `gh` mutations, and persistence surfaces clear through the classifier only on H's explicit instruction. `/doctor` may rewrite `~/.claude.json` and local allowlists; curate durable rules instead of restoring machine state.
- Codex runs the root-denied `trusted-workspace` profile from `templates/codex/config.toml`, installed host-locally by `make stow`, with automatic approval review and command network disabled. The profile grants the mise install directory so the sandbox can execute mise-managed runtimes, `~/Projects` for reading H's repositories and reference clones, the system trees `/usr`, `/etc`, `/opt`, `/sys`, and `/var/lib/pacman` as literal reads, and `/var/tmp` beside the built-in temp grants, with `/tmp/opencode` and `/tmp/claude-1000` denied by literal path; no shape deny can live under a temp root, because the sandbox's glob scan dies on the root-owned entries there, so Codex reads credential-shaped files under the temp roots as it always did and the model refuses them on the guidance. Under `~/Projects`, where the home-wide credential-shape denies keep masking secrets and the shapes those globs cannot cover are denied under `~/Projects` instead, except `.npmrc`, whose glob stalls the sandbox and which therefore stays readable there. Scripted `codex exec` calls use one finite prompt and never `--last`.
- Claude Code and OpenCode carry a read-only `auditor` agent whose charter is `agents/.agents/agents/auditor.md`. Claude Code's agent body is held equal to that charter by the contracts test, with Read, Grep, and Glob only at the highest effort. OpenCode's `agent.auditor` reads the charter and has a fail-closed bootstrap: empty cap slots precede the reserved `** = deny` guard, not copied parent grants. `auditor-permissions.js` intersects the final merged parent read/glob/external-directory policy with agent caps, preserving inherited asks/denies and allowing caps only to tighten them. Missing plugin support leaves the managed bootstrap denied; derivation failure leaves the auditor denied, and an unsupported intersection denies the affected permission. Unknown/custom tools, edits, shell, web, skills, delegation, and MCP read subjects stay denied; grep is deliberately blocked because upstream does not apply per-file read policies to its results. `tests/config-contracts.py` and `tests/opencode-auditor.sh` check the bootstrap and real transform under modeled permission semantics, not live dispatch or containment against later hostile plugins/configuration. Codex has no auditor role yet: its shell sandbox alone would not remove web, apps, plugins, MCP, or nested-agent authority; the ledger holds the live-verification prerequisite.
- OpenCode allows ordinary workspace edits and shell commands. Its managed `external_directory` map defaults to `ask`, preapproving `~/Projects/scratch/**`, `~/Projects/quarry/**`, `/tmp/opencode/*`, `~/.agents/skills/**`, `/usr/**`, and `/var/lib/pacman/**` before sensitive-directory and other-session denies; upstream can append a tool-output location allow. The named OS and quarry reference roots avoid routine diagnostic/research interruptions. Other Projects, `/etc`, `/opt`, `/sys`, and general temp locations still ask. These are location permissions, not read-only grants or OS containment. Native edits outside a non-root Git worktree still ask except for supported managed-scratch edits; persistent scratch and quarry gain no edit exception. With non-Git worktree `/`, `../*` does not match. Destructive Git/configuration asks and existing denies remain. Read/edit subjects are worktree-relative; external subjects name a directory or file's parent plus `/*`. Shell file-command checks and grep do not enforce per-file read/edit policies. Shared guidance owns authorized quarry refreshes and general intent-led collaboration; `/eyrsync` owns refresh preflight. Retain secret-material, edit and native permission boundaries regardless of the location grants.
- OpenCode's separate scratch plugin grants ordinary native create/update/delete under the canonical, caller-owned `/tmp/opencode` only when the normal merged policy and filesystem layout pass preflight. It inserts bounded worktree-relative rules without reordering existing restrictions and validates all scratch targets before native work, rejecting sensitive paths, existing symlink/hardlink escapes, unsafe metadata, and Move-to patches (use Add/Delete). Unsupported layouts retain native permissions. The plugin neither prevents TOCTOU races nor identifies session ownership; same-user sessions share the root, and guidance still limits use to caller scratch. Auditor and later project/agent/session restrictions remain in force.
- OpenCode's `read-permissions.js` adapts only native Read/Glob external-directory fallback asks for ordinary configuration/software/reference categories authorized by shared guidance. The persisted external map omits the redundant `* = ask`; native fallback stays ask, while explicit global/project/agent/session asks remain distinguishable. The adapter matches the exact call/message/session, operand and singleton parent pattern, checks lexical/resolved paths and effective read restrictions, and replies once through the supplied client. It never answers edit, shell, grep or MCP requests and creates no retained location grant. Auditor derivation supplies original-cap provenance bound to the generated policy objects and content; replacements lose that provenance. Credential/session exclusions follow both lexical and resolved XDG roots. Unknown inputs, changed layouts, expired calls or failed policy lookups retain native prompting. Trusted native tools and reviewed plugins are prerequisites; tests do not establish isolation from replacement tools, hostile plugins or filesystem/policy races. Restart OpenCode to activate changes and use `docs/operations.md` for live acceptance.
- Strongest-model/highest-effort preferences remain unchanged. Claude Code's `model`, auditor, and `spar-claude` use the moving `fable` alias. The Codex template and `spar-codex` pin no model, so the catalog's first picker-visible default applies; the ledger holds the strongest-model revalidation trigger, not a guarantee about every invocation. Codex Fast is a service tier, not a picker model: the template leaves `service_tier` unset, keeps `features.fast_mode` for the per-session `/fast` toggle, and `spar-codex` requests standard tier. OpenCode pins concrete primary/mini IDs on standard tier with derived `-fast` variants left to per-session choice; bump pins when its catalog lists a newer generation. `tests/config-contracts.py` enforces these configured shapes and `xhigh` effort. Report effective runtime model/effort/tier only from safe client evidence; configured preferences do not fill unknown provenance fields.

## Reviewer Bridges

- Claude Code reviews with Codex through `spar-codex`. Codex reviews with Claude manually outside its strict profile. OpenCode's configured Claude path is `spar-claude`; active availability is recorded in the maintenance ledger. The bridges, `spar-payload-scan`, and `review-brief`, which assembles the artifact both spar and the auditor take, live in the spar skill's `scripts/` under `~/.agents`, shared by every tool like the skill itself.
- Each bridge runs one read-only, offline, subscription-authenticated, single-turn reviewer from the caller's repository root. Unset `spar.consent` permits review; when set, only literal `true` passes, and empty values or lookup failures refuse. Flags are hard-coded: the reviewer reads the repository except Git internals and credential-shaped paths, has no write, web, MCP, plugin, subagent, or project-instruction surface, starts from a scrubbed environment, and runs under a hard timeout in its own process group. The Codex reviewer uses a read-only, network-off sandboxed shell; the Claude reviewer has only Read, Glob, and Grep. Reply-only stdout stays separate from stderr's validated reviewer ID and safe `model`/`effort`/`tier`/`clientversion` provenance. Unexposed or unsafe values are `unknown`, never inferred from configured preferences.
- Review-recommended paths: a staged change to `claude-code/.claude/settings.json`, `templates/codex/config.toml`, `opencode/.config/opencode/opencode.json`, a bridge, the scanner, `templates/hooks/commit-gate`, a skill script under `agents/.agents/skills/*/scripts`, the OpenCode plugin, or `agents/.agents/shared-guidance.md` is where a spar or auditor review before the packet earns its cost. No review is mandatory. Save in-tool audit evidence under the workstream's `audit/` and cross-vendor spar evidence under `spar/`; the primary records and removes those documents under the shared lifecycle, not the review agents.
- `spar-payload-scan` bounds requests/artifacts, rejects credential-shaped values and sensitive paths (including copied stores), inlines artifacts, and rescans replies. Bridges pass `outbound --root REPO [--scratch-root PRIVATE_TMPDIR] -- FILE...`, keeping artifact operands out of options. Temporary artifacts require an explicitly selected private, owned caller `TMPDIR` below `/tmp` or `/var/tmp`, not a shared tool root; lexical and resolved paths are checked. This selects a private directory, not a new trust tier or proof of session identity. Reviewer runtimes may not resolve inside the repository or temp roots; reviewers receive no scratch-write grant. Addition-only `diff` mode is not complete publication coverage: governance also scans complete new objects and raw commit metadata.

## State and Deployment

- Stow runs with `--no-folding`, so managed parents stay real directories and only leaves are links; each `~/.agents/skills/<name>` is the exception, linked whole because Codex follows directory links but skips file links. Every host-writing Make entrypoint requires the deployed-clone guard. `install-gate` accepts only the fixed canonical `~/.agents/hooks/commit-gate` endpoint outside Git workspaces, with real, non-symlinked parents and an owner-controlled, single-link regular file if the endpoint exists; `GATE` cannot relocate it. `check-skills` preflights all selected skills before cleanup/conversion; foreign entries refuse unchanged. Cleanup removes only recognized dangling package links and retired directories left empty. `.NOTPARALLEL` serializes one Make invocation, not independent deployment processes or disk failures.
- `verify-deploy` uses preparation's read-only `--check-gate` mode, sharing installation's fixed-endpoint, parent, ownership and link checks, then requiring executable content equal to the template. A missing or unsafe gate refuses before other deployment verification; verification never installs or repairs it.
- Codex reconciliation installs a host-local file: the template owns root keys and its tables; host-only tables and `hooks.state` survive with their parsed semantics verified before replacement bytes are emitted. Complete TOML prefixes distinguish real sections from multiline values; unsupported inline/dotted trust-state layouts refuse without writing or printing values. Root `service_tier` stays host-owned while the template omits it, unless the host root exactly matches a committed template root; that textual residue rule remains, so an uncommitted-template root retains the key. `make verify` checks the installed template boundaries, not host-secret contents in reports.
- Run authorized host gates through their reviewed entrypoints when native permissions allow: normal reconciliation keeps merged configuration in a private local file and exposes structural checks, not raw values. Do not turn possible secret-bearing host state into a blanket manual handoff, but never open/dump that state or run `merge` against a host file into visible output. Actual access refusals, unsafe diagnostic paths, or repairs requiring secret inspection remain blockers; no tool or permission bypass is authorized.
- Hermes configuration is also potentially credential-bearing. The primary profiles, reviewer bridges and scanner protect `.hermes/config.yaml` alongside existing `.env`/`auth.json` rules. Use only the opaque reconciler for host configuration; authentication is H's interactive `hermes model` step. Python with PyYAML is required for repository and deployment checks. Restart Hermes after deployment, and OpenCode after its permission changes.
- The Hermes canary uses ordinary `--cli chat --source tool --quiet --query`, never top-level `--oneshot`, which bypasses approvals in v0.19. The source tag keeps automation out of ordinary continuation selection. It skips inherited bypass/customization-disable flags or alternate profiles. Interactive approval prompts and asynchronous child completion remain separate acceptance cases. The native model wizard's canonical Codex base URL and an agreeing model alias are preserved by reconciliation; model.default/provider remain template-owned.
- Empty package directories are not tracked; a directory appears in a package only when it holds a managed file.
- A change that alters deployed state, such as the package layout, link targets, or the host Codex config, is deployed on the host where it is made, and the same commit adds a host pass item to `docs/maintenance.md` with the exact steps for the other host; the agent runs that item in the next session on that host, after H pulls, and removes it when done.
- The gates follow the `commit` skill's contract: `make lint/check` are repository checks and `make restow/verify` host checks. Node.js is an EyrAgents verification prerequisite, not EyrWSL's baseline. `make canary` is separate behavioral smoke: up to six calls per tool, successful nonempty replies required, with fixture HEAD preservation. OpenCode uses a local README and performs the preapproved `/usr/lib/os-release` read; only its general external-temp read stays interactive-only and skipped. Failure exits 1, skipped/unverified checks 2, and success 0 is not independent dispatch proof. Restart OpenCode after config/skill changes. Keep genuinely pending host evidence and revalidation triggers in the ledger.

## Skills

- `commit`: run the repository's gates, then stage, review, and commit one exact atomic change with H's approval.
- `publish`: review the exact commits a push, release, or pull request would expose, then verify the push and the published state after H runs it.
- `spar`: value-based cross-model review through a read-only reviewer bridge.
- `eyrsync`: this repository's own skill, under `.agents/skills` with a `.claude/skills` symlink, since Codex and OpenCode discover `.agents/skills` natively. Check reference/evidence coverage for every tool, then reconcile official docs/releases, role-appropriate source/public material, runtime evidence and the Agent Skills specification; sync siblings where their contracts depend on the harness.
- `omarchy`: required for end-user Linux desktop, Hyprland, Omarchy, terminal, theme, and display configuration. Omarchy installs it and `diagnose-crash` as links under `~/.claude/skills` and `~/.agents/skills`; they resolve outside the packages, and `make clean` never touches them.
