---
description: Review the exact commits a push, release, or pull request would expose
agent: build
---

Use the `publish` skill. Review every binding and present one compact summary and one approval selector for the fixed ordered push set. Keep full IDs, argv and evidence in the technical records, with material exceptions visible. H's set approval permits one `publish-apply ID` invocation per listed binding, executed and verified separately in order. Obtain native execution authority and preflight the set before starting. Stop on failure/drift; the unattempted remainder needs fresh approval. Never replay an attempted ID or infer approval from a receipt, details request or clipboard copy. Commit approvals remain individual.
