---
description: Review the exact commits a push, release, or pull request would expose
agent: build
---

Use the `publish` skill. Review the exact publication delta and present its repository/binding-ID packet. Only H's contemporaneous approval of that exact binding authorizes one `publish-apply ID` invocation, followed by separate verification. Obtain necessary native execution authority before invoking the helper. Never replay an attempted ID or infer approval from a receipt or clipboard copy.
